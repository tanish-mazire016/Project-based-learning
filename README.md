# Tech_Fiesta_Project

FrontEnd run Steps:
1> npm run dev

Backend Run Steps:
1> pip install fastapi uvicorn (for the first time only!)
2>python -m uvicorn main:app --reload (Always)

To refresh or get updated data use --     git pull origin main 
